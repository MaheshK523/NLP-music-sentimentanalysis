[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[project]
name = "music-emotion-analysis"
version = "0.1.0"
description = "Offline-first, reproducible emotion analysis for lyric datasets"
readme = "README.md"
requires-python = ">=3.10"
